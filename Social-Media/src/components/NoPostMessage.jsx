import style from "../components/NoPostMessage.module.css"
const NoPostMessage = () => {
  return (
    <>
      <h2 className={`${style.editheader}`}>No Post Available</h2>
    </>
  );
};

export default NoPostMessage;
